# ===== PostgreSQL =====
# Diese Variablen werden sowohl vom Postgres-Container als auch von n8n genutzt
N8N_PG_USER=n8n
N8N_PG_PASSWORD=change-me
N8N_PG_DATABASE=n8n

# Optional aber empfohlen: Verschlüsselungsschlüssel für n8n (>= 32 Zeichen)
# Beispiel: macOS -> LC_ALL=C tr -dc 'A-Za-z0-9' </dev/urandom | head -c 40
N8N_ENCRYPTION_KEY=

# ===== n8n Host/URLs =====
# Der Reverse-Proxy (Caddy) liefert HTTPS für localhost (Google OAuth kompatibel)
N8N_HOST=localhost
N8N_WEBHOOK_URL=https://localhost

# ===== Zeitzone =====
GENERIC_TIMEZONE=Europe/Berlin


